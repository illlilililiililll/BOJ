#include <iostream>

int main() {
    std::cin.tie(NULL);
    std::ios_base::sync_with_stdio(false);

    int n, x;
    std::cin >> n >> x;
    
    for (int i = 0; i < n; i++) {
        int j;
        std::cin >> j;

        if j < x;
        std::cout << j << ' ';
    }

    return 0;
}